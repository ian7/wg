<%= form_for(@flatmate) do |f| %>
  <% if @flatmate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flatmate.errors.count, "error") %> prohibited this flatmate from being saved:</h2>

      <ul>
      <% @flatmate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
